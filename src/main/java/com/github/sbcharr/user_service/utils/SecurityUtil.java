package com.github.sbcharr.user_service.utils;

public class SecurityUtil {
    // generate the jwt token prefix (algorithm)
    public static String jwtTokenPrefix() {
        return null;
    }

    // generate the jwt token body (header + payload)
    public static String jwtTokenBody() {
        return null;
    }

    // generate the jwt token suffix (signature). Signature helps to validate the token
    public static String jwtTokenSuffix() {
        return null;
    }
}
